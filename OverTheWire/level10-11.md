Bandit Level 10 → 11
Commandes utilisées :
cat data.txt
base64 -d data.txt

Notes :
Le mot de passe était encodé en base64.
Il fallait le décoder avec base64 -d.
Premier test avec l’encodage, plutôt cool.
